<p id="notice"><%= notice %></p>

<h1>Agents</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @agents.each do |agent| %>
      <tr>
        <td><%= link_to agent.name, masteragent_agent_subagents_path(agent.masteragent_id ,agent.id) %></td>
        <td><%= link_to 'Show', masteragent_agent_path(agent.masteragent_id ,agent.id) %></td>
        <td><%= link_to 'Edit', edit_masteragent_agent_path(agent.masteragent_id ,agent.id) %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Agent', new_masteragent_agent_path %>
