<h1>Editing Shop</h1>

<%= render 'form', shop: @shop %>

<%= link_to 'Show',  masteragent_agent_subagent_shop_path(@masteragent, @agent, @subagent, @shop) %> |
<%= link_to 'Back', masteragent_agent_subagent_shops_path(@masteragent, @agent, @subagent) %>
